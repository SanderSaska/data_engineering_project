-- Create the raw_data database
CREATE DATABASE raw_data;

-- Optional: Grant privileges
-- GRANT ALL PRIVILEGES ON DATABASE raw_data TO airflow;


-- to access raw_data database
-- psql -U airflow -d raw_data -h localhost
